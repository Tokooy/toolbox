/* ============================================================
   宿主 SDK · 运行时依赖
   ------------------------------------------------------------
   Vue 与 ECharts 由外壳 index.html 以**本地经典脚本**加载
   （离线可用、无 CDN、无构建步骤）。模块脚本天然在经典脚本之后
   执行，因此这里可以直接取到全局对象。

   应用面板这样使用：
     import { Vue, echarts } from '/static/sdk/runtime.js';
   ============================================================ */

export const Vue = window.Vue;
export const echarts = window.echarts;
